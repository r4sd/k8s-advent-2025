apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secure-pod.yaml
  # insecure-pod-example.yaml is intentionally excluded
  # It's for documentation/learning purposes only
